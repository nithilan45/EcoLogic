# Dependencies for the quality/energy evaluation (benchmark/, router_v2/,
# stage7_10/, stage11_13/). The app itself needs only requirements.txt; nothing
# here is imported by backend/main.py.
#
# Versions are the ones the reported results were produced with, per
# stage7_10/reproducibility_manifest.md. Note that pinning these does NOT make
# results bit-reproducible: none of the three model providers expose an
# immutable model revision, and the tiers are measurably nondeterministic even
# at temperature 0 (see stage7_10/s7_generation_variance.md).
#
# Python 3.12.3, CPU only. No GPU is used anywhere; the local embedding model
# runs on CPU.
#
# The CPU torch build is not on PyPI, so the extra index below is required;
# without it pip fails with "No matching distribution found for torch==2.14.0+cpu".
--extra-index-url https://download.pytorch.org/whl/cpu

# API calls to Together AI and OpenAI
httpx==0.28.1

# analysis, statistics, LP/MILP frontiers
numpy==2.4.4
scipy==1.18.1
scikit-learn==1.9.0
matplotlib==3.11.1

# benchmark item sets are distributed as parquet
pyarrow==25.0.1
huggingface_hub==1.30.0

# router variant R2: local sentence embeddings (all-MiniLM-L6-v2), CPU
sentence-transformers==6.0.1
transformers==5.16.1
tokenizers==0.23.2
torch==2.14.0+cpu

# Stage 9 (external check against RouteLLM's released data) and Stage 11, where
# the RouterBench releases ship as pickled DataFrames. Note `datasets` is NOT
# required: the pickles are read with pandas directly.
pandas==3.0.5
tiktoken==0.14.0

# Stage 13: fine-tuning jobs, endpoint probes and the prompted LLM router. The
# raw REST API would do, but the SDK is what the recorded provider errors in
# s13_ftblocked.json and s13_endpoint_probe.json came from, including the v2
# `models.configs.list` and deployment calls that have no documented REST form.
together==2.32.0
